严格遵守下面的规则：
1. 每次任务完成后，都要迭代app version，更新app中“设置”-“关于”里的版本号，并且把更新记录写入到UpdateRecord.md文件中
2. 所有的回复都使用中文
3. 每次编译后都要把历史版本的apk文件保存到D:\code\离线听书\history_version文件夹中，文件名格式为：version_name.apk
4. 每次都要阅读UpdateRecord.md文件，了解上一次的更新内容,确保不要重复更新，不要导致之前的BUG再次发生
